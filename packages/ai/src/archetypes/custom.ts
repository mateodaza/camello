import { registerArchetype } from '../archetype-registry.js';

registerArchetype({
  type: 'custom',
  prompts: null,
  defaultTone: { en: '', es: '' },
  moduleSlugs: [],
  icon: 'Sparkles',
  color: 'charcoal',
  ragBias: null,
});
