[build]
dockerfilePath = "apps/jobs/Dockerfile"
watchPatterns = ["/apps/jobs/**", "/packages/**"]

[deploy]
startCommand = "node main.js"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
